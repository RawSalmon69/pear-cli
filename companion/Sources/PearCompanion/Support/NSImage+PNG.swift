import AppKit

extension NSImage {
    /// PNG encoding at the image's pixel dimensions. Nil if there's no bitmap.
    func pngData() -> Data? {
        guard let tiff = tiffRepresentation,
              let rep = NSBitmapImageRep(data: tiff) else { return nil }
        return rep.representation(using: .png, properties: [:])
    }
}
